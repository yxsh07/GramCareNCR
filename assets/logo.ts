// Point to a static image in the public folder. Place your `logo.png` at `public/images/logo.png`.
// Prefer the user's provided PNG logo if present. If the PNG isn't available the Header component
// will fall back to the shipped SVG at /images/logo.svg via an onError handler.
export const logoBase64 = '/images/logo.png';
